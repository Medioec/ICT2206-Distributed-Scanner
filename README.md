# ICT2206 Distributed Scanner

Install requirements:
pip install -r requirements.txt

Make sure the tool is run on a linux host (WSL not supported)

Make sure port 6443 on host is reachable from the internet
